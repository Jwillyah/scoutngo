# Copy to .env and fill in your own key. .env is gitignored and never committed.
# Used only by the serverless function that proxies the Anthropic API, so the
# key stays out of the browser bundle.
ANTHROPIC_API_KEY=
